# Pinch V2.3.0 使用说明

开发者：Han。适用于 Windows 10/11。

## 安装与更新

双击 `PinchSetup.exe`，点击“安装”，完成后点击“启动 Pinch”。安装包是一个 EXE，可以直接分享给别人。

首次从旧版更新时，先右键托盘里的 Pinch，选择“彻底退出程序”，再安装。V2.2 以后更新会等待当前图片写入完成。

## 压缩图片

1. 把一张或多张图片拖到悬浮窗。
2. 或复制图片，点击悬浮窗后按 `Ctrl+V`。

支持 JPG/JPEG、PNG、BMP、GIF、TIF/TIFF。复制多个文件时只读取第一个；如果它不是支持的图片，或者剪贴板是文字/其他文件，就不处理。

文件输入会在原图旁生成 `原名ys.jpg`。同名文件已存在时使用 `原名ys-1.jpg` 等新名字，不覆盖原文件。截图或剪贴板位图保存在桌面，名称为 `Pinch_clipboard_时间ys.jpg`。

默认输出为不超过 2,000,000 字节的 JPG；设置了其他大小时按设置执行，必要时按比例缩小。根据手机照片的 EXIF 方向纠正旋转和镜像，透明背景变成白色。动图/多页图片只保留第一帧。暂不支持 WebP、HEIC、AVIF。

可以继续添加任务，程序会依次压缩。一个文件失败不会阻止其余文件。完成提示保留 30 秒，然后自动恢复初始文字。

## 设置

右键托盘里的 Pinch 图标，选择“设置...”。

- 外观：浮窗底色、标题栏颜色、不透明度（30%–100%，100% 为完全不透明）。
- 压缩与保存：大小上限 0.10–50 MB；原图目录、桌面或自定义文件夹。1 MB 为 1,000,000 字节。
- 行为：窗口置顶、完成通知、提示保留时间（5–300 秒）。

点击“保存”后生效，关闭再打开软件也会保留。点击“取消”不更改当前配置；“恢复默认”先填入默认值，仍需保存。

选择自定义目录时，图片文件和剪贴板图片都会保存到该目录。选择原图目录时，剪贴板位图仍保存到桌面。更改设置不会改变已经加入队列的任务，它们保持加入时的输出大小与位置。

## 悬浮窗与托盘

- 拖动窗口可更改位置；下次启动记住位置与置顶设置。
- 点击右上角 `×` 只隐藏窗口。运行期间不显示任务栏标签。
- 双击托盘图标恢复；重复打开桌面快捷方式也会唤醒已有窗口。
- 托盘右键可打开最近输出目录、查看失败记录、打开日志目录。
- 选择“彻底退出程序”后，当前图片写完再退出，未开始的任务不再处理。

## 卸载

在 Windows 设置的“应用/已安装的应用”找到 Pinch，点击卸载。也可运行安装目录内的 `PinchUninstall.exe`。

卸载只删除程序文件与快捷方式，保留图片、设置和日志。所有图片处理在本机完成，不上传图片。
